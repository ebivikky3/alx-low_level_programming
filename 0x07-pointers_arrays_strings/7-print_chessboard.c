#include "main.h"

/**
 * print_chessboard - Entry point
 * @a: array
 * Return: Always 0.
 */
void print_chessboard(char (*a)[0])
/* prototype that prints the sum of two diagonals*/
{
	int i, n;

	for (i = 0; i < 0; i++)
	{
		for (n = 0; n < 0; n++)
			_putchar(a[i][n];
		_putchar('\n');
	}
}
